application prod properties

# Additional DB Configurations
# ===
spring.jpa.hibernate.ddl-auto=update
logging.level.root=ERROR

# Authentication Configuration (Clerk)
# ===
auth.validator.type=clerk

clerk.jwks.url=${CLERK_JWKS_URL:https://sacred-raccoon-10.clerk.accounts.dev/.well-known/jwks.json}

# CORS set to your Vercel frontend URL on Render once deployed
# e.g. CORS_ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3001
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3001,https://skillmentor-frontend-tau.vercel.app/}

